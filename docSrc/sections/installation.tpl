The plugin SDK can be fetched from the npm registry ({{pluginNpmUrl}}). You can use npm command line tool to install it locally.

    npm install ({{pluginNpmName}})

You can also save the module as a local dependency in your project package.json file with the following command:

    npm install --save ({{pluginNpmName}})
